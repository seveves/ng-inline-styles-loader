{
  "name": "ng-inline-styles-loader",
  "version": "0.0.2",
  "description": "A webpack loader for inlined stylesheets of angular components.",
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter spec",
    "coverage": "istanbul cover ./node_modules/mocha/bin/_mocha -- -R spec",
    "preversion": "npm test",
    "version": "git add -A",
    "postversion": "git push && git push --tags",
    "release:patch": "npm version patch && git push && git push --tags && npm publish"
  },
  "keywords": [
    "angular",
    "webpack",
    "loader",
    "ng"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/seveves/ng-inline-styles-loader.git"
  },
  "author": "Severin Friede <sevefriede@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "autoprefixer": "^6.7.2",
    "clean-css": "^4.0.6",
    "loader-utils": "^0.2.16",
    "postcss": "^5.2.12"
  },
  "devDependencies": {
    "codecov": "^1.0.1",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "should": "^11.2.0"
  }
}
